apiVersion: 1

providers:
  - name: Default    # A uniquely identifiable name for the provider
    folder: '' # The folder where to place the dashboards
    type: file
    editable: true
    allowUiUpdates: true
    options:
      path:  /etc/grafana/provisioning/dashboards/sysadmin.json
